# yaml-language-server: $schema=https://sap.github.io/ui5-tooling/schema/ui5.yaml.json

# Minimal UI5 tooling descriptor for this CAP database adapter.
#
# `cap-snowflake` is a server-side `cds-plugin` (CDS database adapter) and
# does not ship UI5 tasks, middlewares, or a UI5 module. This file exists
# so that tooling that scans for `ui5.yaml` (e.g. bestofcapjs crawler) can
# identify the project.
specVersion: "3.0"
metadata:
  name: cap-snowflake
type: module
